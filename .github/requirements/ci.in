# What the CI test job installs; ci.txt is generated from this file (the command is in ci.yml).
# Runtime dependencies (pyproject.toml), the gui and mcp extras, and the test tools.
requests
Flask
httpx
defusedxml
PySide6==6.11.2
qt-material
mcp>=1.28.1,<3
pytest
pytest-asyncio
pytest-timeout
